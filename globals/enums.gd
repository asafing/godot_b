class_name Enums

enum SkillType { Damage, Buff }

enum PossibleTargets { AllOthers, All, Ally, Enemies, Conditional }

enum MonType { Enemy, Player }

enum TurnState { NONE, PRE_TURN, ACTIONS_SELECT, PLAY_ACTIONS, POST_TURN }
